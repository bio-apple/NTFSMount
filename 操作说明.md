# NTFS 读写 · 操作说明

在这台 Mac 上读写 Windows（NTFS）移动硬盘。菜单栏图标名：**NTFS**。

用户态依赖（`ntfs-3g`、FUSE 库、`go-nfsv4`）都在 **NTFSMount.app** 里，不必再单独装 FUSE-T。  
仍需安装一次挂载助手（管理员密码），用于打开磁盘设备。

---

## 日常用法（最常用）

1. **插入** NTFS 移动硬盘（如 Elements SE）。
2. 点菜单栏 **NTFS**。
3. 在对应硬盘上选 **以可写方式挂载**  
   （多块盘可点 **全部以可写方式挂载**）。
4. 访达打开 **/Volumes/硬盘名**（例如 `Elements SE`），正常复制、删除、改名。
5. 用完先点 **推出（可安全拔出）**，等盘消失后再拔线。

不要直接拔线。不推出就拔，下次可能变成脏盘，甚至丢数据。

---

## 菜单状态含义

| 标记 | 含义 | 你要做什么 |
| --- | --- | --- |
| ● 可写 | 已经能读写 | 直接用访达 |
| ○ 系统只读 | macOS 自己挂的，只能看 | 点「以可写方式挂载」 |
| ◌ 未挂载 | 盘在，还没挂上 | 点「以可写方式挂载」 |
| … | 正在处理 | 等几秒 |

其他菜单项：

- **在访达中打开**：打开当前挂载点  
- **卸载**：卸掉可写挂载（盘还插着）  
- **推出（可安全拔出）**：卸载并通知系统可拔线  
- **刷新**：重新扫描硬盘  
- **开机启动**：登录后自动出现菜单栏图标  

---

## 第一次使用 / 重装

```bash
cd ~/Desktop/bio-apple/NTFSMount
./install.sh
```

输入一次管理员密码。之后挂载通常不再要密码。菜单栏应出现 **NTFS**。

`install.sh` 会把下列文件打进 `/Applications/NTFSMount.app/Contents/MacOS/`：

- `NTFSMount`（菜单栏）
- `ntfs-3g`
- `libfuse.2.dylib`（FUSE-T 兼容层）
- `libntfs-3g.90.dylib`
- `go-nfsv4`

并安装 `/usr/local/sbin/ntfs-rw-helper` + sudo 规则。

---

## 权限（出问题时再开）

1. **完全磁盘访问权限**  
   系统设置 → 隐私与安全性 → 完全磁盘访问权限 → 打开 **NTFSMount**

2. **网络卷**  
   系统设置 → 隐私与安全性 → 文件与文件夹 → 打开 **网络卷**  
   （挂上了但访达列不出内容时）

---

## 注意

- 在 Windows 上尽量 **正常关机** 再拔盘。脏盘第一次可能用 `force` 挂载（会清休眠文件），重要数据请先备份。
- 若同时出现 **Elements SE** 和 **Elements SE 1**：带「1」的多半是系统只读；请用菜单挂上的可写卷。
- 拔掉重插后盘符可能从 `disk4s1` 变成 `disk5s1`，属正常。

---

## 自检（可选）

```bash
mount | grep 'Elements SE'
printf 'ok\n' > "/Volumes/Elements SE/.ntfs_check.txt" && rm "/Volumes/Elements SE/.ntfs_check.txt" && echo 可写正常
pgrep -lf 'NTFSMount.app/Contents/MacOS/go-nfsv4|NTFSMount.app/Contents/MacOS/ntfs-3g'
```

期望挂载类似：`fuse-t:/Elements SE on /Volumes/Elements SE (nfs)`，且进程路径在 app 包内。

---

## 卸载

```bash
cd ~/Desktop/bio-apple/NTFSMount
./uninstall.sh
```

---

## 一句话记住

**插盘 → 菜单「可写挂载」→ 用完「推出」→ 再拔线。**
